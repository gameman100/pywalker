字段格式:
    字段名:类型
例如：
    id:i  name:s

========================
字段类型：
i  # int(整型)
f  # float(浮点型)
d  # double(双精度浮点型)
b  # bool(布尔型)
s  # string(字符串)
arr  # arr(整型数组，每个数据用“,”号隔开， 如 4,3,5,6,7)
dic  # dic(整型字典，每对键值用“:”号分开，每对数据用“,”号隔开， 如 1：12，3：5)
